function rsvpReducer(state = {events: [], eventsAttending: [], isLoading: false, errorMessage: ''}, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default rsvpReducer;
